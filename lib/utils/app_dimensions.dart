class AppDimensions {
  const AppDimensions._();

  /// t size, contains [tiny] interval sizes
  static const t = 1.0;
  static const xt = 4.0;

  /// s size, contains [small] interval sizes
  static const xs = 10.0;
  static const xxs = 15.0;

  /// m size, contains [medium] interval sizes
  static const xxxm = 36.0;

  /// l size, contains [large] interval sizes

  /// h size, contains [huge] interval sizes
  static const h = 150.0;
  static const xh = 300.0;

  static const padding = AppDimensions.xs;
  static const margin = AppDimensions.xs;
  static const radius = AppDimensions.xs;
  static const textSize = AppDimensions.xxs;

  static const elevatedButtonRadius = AppDimensions.xxxm;
  static const elevatedButtonNameVerticalPadding = AppDimensions.xxs;
  static const elevatedButtonNameHorizontalPadding = AppDimensions.xxxm;
  static const elevatedButtonNameSize = AppDimensions.xxs;
}
